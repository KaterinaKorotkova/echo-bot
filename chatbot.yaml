# Название проекта
name: echo-bot

# Точка входа
entryPoint: main.sc

botEngine: v2 # версия диалогового движка
language: ru # язык бота

tests:
  include:
    - test.xml

nlp:
  intentNoMatchThresholds:
    phrases: 0.2
    patterns: 0.2
